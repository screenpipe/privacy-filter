fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
# CUDA-enabled torch wheels from PyPI — bundled with CUDA 12.4 runtime,
# matching the nvidia/cuda:12.4.1-cudnn-runtime base image.
torch==2.5.1
# openai/privacy-filter ships a custom `OpenAIPrivacyFilterForTokenClassification`
# arch + `TokenizersBackend` tokenizer class; both require transformers >= 5.x.
transformers==5.6.0
tokenizers==0.22.1
accelerate==1.1.1
safetensors==0.4.5
# rfdetr_v8 image-PII detector. GPU EP via CUDA + TensorRT (the gpu
# wheel includes CPUExecutionProvider as a final fallback).
onnxruntime-gpu==1.20.1
pillow==11.0.0
# numpy is pulled in transitively by torch but pin it explicitly so a
# torch minor bump can't drift the version we test against.
numpy==2.1.3
