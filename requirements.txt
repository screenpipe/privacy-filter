fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
# Kept for standalone/local installs. The Tinfoil image inherits its CUDA and
# torch stack from the pinned confidential-vLLM base in Dockerfile.
torch==2.5.1
# transformers is used ONLY for AutoTokenizer — the text redactor runs
# the INT8 ONNX directly via onnxruntime (raw session + manual BIO decode,
# see server.py::_opf_on_batch), so optimum/opf are no longer needed.
# Pinned to 4.46.x: the last line compatible with huggingface_hub 0.26.2
# (transformers 5.x needs hub>=1.5; optimum needs transformers<5).
transformers==4.46.3
tokenizers==0.20.3
accelerate==1.1.1
safetensors==0.4.5
# Text + rfdetr_v19 image-PII inference. The pinned Tinfoil/vLLM base is CUDA 13;
# ONNX Runtime 1.20.x wheels target CUDA 12 and silently fell back to CPU.
# 1.29.x is the stable CUDA 13 + cuDNN 9 wheel line.
onnxruntime-gpu==1.29.0
pillow==11.0.0
# numpy is pulled in transitively by torch but pin it explicitly so a
# torch minor bump can't drift the version we test against.
numpy==2.1.3
# httpx is the async client used by server.py to reverse-proxy /v1/*
# requests to the co-hosted Gemma vLLM container at 127.0.0.1:8001.
# Pinned because patches between minor versions have changed how
# streaming generators close — SSE behavior matters here.
httpx==0.27.2
